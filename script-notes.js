// Go through raw.errors, raw.errors is a report of the json file ???
// Question: what does .each's 2nd parameter consist of ? What is error ? index is self-explanatory.

/* Temp object consists of 
    Error Type
    Cuboid ID
    Nuro ID
    Frames
    Dimensions
    Position
    Label
*/

// Debug ???  Line 31
    // console.log(index)
    // console.log('error type:', error.error_type)
    // let cuboidId = error.original ? error.original.uuid : error.error_type
    // console.log('cuboidID: ', cuboidId);